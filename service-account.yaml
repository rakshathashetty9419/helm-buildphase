apiVersion: v1
kind: ServiceAccount
metadata:
  name: ${{ vars.SERVICEACCOUNT_NAME }}
  namespace: default
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: hello-world-role
  namespace: default
rules:
- apiGroups: [""]
  resources: ["pods"]
  verbs: ["get", "watch", "list", "create", "delete"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: hello-world-rolebinding
  namespace: default
subjects:
- kind: ServiceAccount
  name: ${{ vars.SERVICEACCOUNT_NAME }}
  namespace: default
roleRef:
  kind: Role
  name: hello-world-role
  apiGroup: rbac.authorization.k8s.io
